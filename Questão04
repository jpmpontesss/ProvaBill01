int main()
{
    int valor, contador=0;

    printf("Digite um número\n ");
    scanf("%i", &valor);

    while(valor >= contador)
    {
        printf("%i", valor);
        valor--;
    }
    
    return 0;
}
